/**
 * Function that checks whether two strings are equal when both are lowercase
 * @param {string} firstValue - the first string to check
 * @param {string} secondValue - the second string to check
 */
const equalsLowerCase = (firstValue: string, secondValue: string) =>
  firstValue.toLowerCase() === secondValue.toLowerCase()

export default equalsLowerCase
