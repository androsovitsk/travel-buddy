/**
 * Function to convert a degree to radian.
 * @param {number} degree - the degree to convert
 */
const convertDegreeToRadian = (degree: number) => (degree * Math.PI) / 180

export default convertDegreeToRadian
