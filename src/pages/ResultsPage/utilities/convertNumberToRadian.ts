/**
 * Function to convert a number to radian.
 * @param {number} number - the number to convert
 */
const convertNumberToRadian = (number: number) => (number * Math.PI) / 180

export default convertNumberToRadian
