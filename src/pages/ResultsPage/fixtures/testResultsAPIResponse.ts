import IResultsAPIResponse from '../types/IResultsAPIResponse'

const testResultsAPIResponse: IResultsAPIResponse = {
  destinations: ['Paris', 'Lyon'],
  distances: [391],
  numberOfPassengers: 1,
  selectedDate: '2023-01-01'
}

export default testResultsAPIResponse
